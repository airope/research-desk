# User catalogue example

`catalogue.csv` is entirely simulated input for the user import workflow. It contains no real publication claims. Preserve local_id when exporting. Authors are separated with semicolons; CSV columns themselves use commas. Empty optional fields are allowed. The in-app demonstration supplies a separate incoming list with curated duplicate, new-publication and conflicting-metadata cases.
